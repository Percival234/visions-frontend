// import type { Mail } from '@/types/entities/mail.type';

// import { ApiService } from '@/services/api-services/api-service';

// import { axiosInstance } from '@/api/api-instance';

// class MailService extends ApiService {
//   constructor(private path = '/mail') {
//     super();
//   }

//   async get(): Promise<Mail> {
//     return (await axiosInstance.get(this.path)).data;
//   }
// }

// export const mailService = new MailService();
